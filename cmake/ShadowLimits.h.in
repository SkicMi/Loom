#pragma once
#include <cstdint>

//=============================================================================================
//  GENERIRAN FILE - ne uredivati. Izvor je CMakeLists.txt.
//
//  Ovi brojevi moraju biti isti u C++u i u shaderima. Prije su stajali na tri mjesta i
//  sinkronizirali se rukom, uz komentar koji je molio da se ne raziđu - a da se raziđu,
//  pipeline bi se svejedno preveo i citanje polja bi otislo izvan granica. Tiho.
//
//  Sada CMake drzi broj na jednom mjestu: odavde ide u C++ kroz ovaj header, i u shadere
//  kroz -D na slangc. Razici se vise ne mogu.
//=============================================================================================

//Koliko ravnih shadow karata nosi set 0
inline constexpr uint32_t maxShadowMaps = @LOOM_MAX_SHADOW_MAPS@;

//Koliko kocaka. Kocka je sest lica, pa je skuplja sest puta i ima ih manje
inline constexpr uint32_t maxShadowCubes = @LOOM_MAX_SHADOW_CUBES@;
